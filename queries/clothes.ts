import Cloth from "@/model/cloth-model";
 

import {ICloth} from "@/types/types"

export async function storeCloth(
  clothInput: ICloth
) {
  const {altTag, price, description, imageUrl} = clothInput;
}
